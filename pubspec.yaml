name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.3.0
  firebase_core: ^3.13.0   # Asegúrate de usar esta versión
  firebase_auth: ^5.5.3  # Usa esta versión para que sea compatible con firebase_core
  cloud_firestore: ^5.6.6
  intl: ^0.20.2
  firebase_storage: ^12.4.5  
  image_picker: ^1.0.7 


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/organic_farming.jpg
    - assets/icons/home_icon.png
    - assets/icons/leaf_icon.png
    - assets/icons/notification_icon.png
    - assets/icons/group_icon.png
    - assets/sustainable_farming.jpg
    - assets/organic_farming_2.jpg


  # Agrega aquí la ruta de tus assets, en este caso las imágenes.
